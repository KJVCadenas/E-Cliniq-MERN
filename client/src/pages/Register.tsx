import { RegisterForm } from '../components/forms/RegisterForm';

export default function Register() {
  return (
    <div className="min-h-screen w-full bg-clinic-yellow-dark flex items-center justify-center">
      <RegisterForm />
    </div>
  );
}
